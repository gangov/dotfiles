return {
  'akinsho/toggleterm.nvim',
  version = '*',
  opts = {
    -- options
    open_mapping = [[<C-CR>]],
    hide_numbers = true, -- hide the number column in toggleterm buffers
    direction = 'float',
    shell = 'fish',
  },
}
